Stage 0 — Getting Started
=========================

Covers the "hello world" baseline (Django start page inside Electron).

Outline:

1. Prerequisites (Python 3.14, uv, Node)
2. Run ``just install`` + ``just electron-install``
3. Launch ``npm start`` / ``just electron-start``
4. Troubleshooting tips (firewall, port conflicts)
