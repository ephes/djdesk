Stage 1 — Workspace Setup
=========================

Guided onboarding wizard for selecting a workspace folder and seeding demo data.
Document how Electron's file picker feeds Django models and where settings are
stored locally.

Checklist:

- Capture screenshot of the wizard from the tutorial build (feature flag: ``FEATURE_WORKSPACE``)
- Step-by-step instructions for naming workspace, choosing directory, loading fixtures
- Mention CLI alternative (``uv run python manage.py setup_workspace``) for automation
