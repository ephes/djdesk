Stage 4 — Task Runner & Assistant
=================================

Walk through submitting jobs via `django-tasks`, monitoring the progress bar, and
chatting with the assistant panel.

Cover:

- UI entry points (assistant quick actions, toolbar badge)
- Backend endpoints invoked (REST + WebSocket updates)
- How to cancel, retry, and view logs for a given task
